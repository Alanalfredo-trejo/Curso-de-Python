tupla = tuple(["dato1","dato2"])

tupla = "dato1", "dato2"

tupla = "dato",

print(tupla)

#Una tupla es una estructura de datos ordenada e
#inmutable (que no se puede modificar una vez creada) utilizada para almacenar una colección de elementos, generalmente de distintos tipos.